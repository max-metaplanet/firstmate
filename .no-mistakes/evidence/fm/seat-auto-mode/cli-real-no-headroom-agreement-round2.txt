## Round 2 live: real quota-axi, ambient default seat active, real named seat seat-max as the only candidate
## destination-min 99 so seat-max cannot qualify; threshold 100 so the trigger is crossed; plan quota on default is above 0
$ fm-seat.sh threshold 100
auto-switch threshold: 100% left
[exit 0]
$ fm-seat.sh destination-min 99
destination minimum: 99% left
[exit 0]
$ fm-seat.sh extra-usage stop
extra-usage policy: stop
new Claude workers are held once the active seat has no plan quota left.
This stops STARTING new work. It cannot stop a worker already running from
drawing extra usage mid-task, so it is not a guarantee of zero spend.
[exit 0]
$ fm-seat.sh auto
claude-seat: default is at 9% left and no seat has enough headroom to switch to; new Claude work will be held once this seat's plan quota runs out, rather than started on paid extra usage. A worker already running is not stopped.
[exit 0]
$ fm-seat.sh status
active seat for NEW workers: default
active profile: (ambient default login)
login state: unknown
auto-switch trigger: at or below 100% left on the active seat
destination minimum: only switch to a seat above 99% left
extra-usage policy: stop - hold new Claude dispatch rather than start work on paid extra usage
auto-switch watch: not armed
new Claude dispatch: HELD (bin/fm-spawn.sh --ignore-seat-hold starts one task anyway)
  the active Claude seat quota could not be read, so whether this worker would run on paid extra usage is unknown, and the policy makes no guess
  this holds only work not yet started; a worker already running keeps its own seat and can still draw extra usage mid-task

live workers keep the seat they launched with:
  (no task records)

local secondmate homes declining inherited seats:
  (none - every local home takes this seat)
[exit 0]
## repeat poll, nothing changed: expect silence (one wake per crossing)
$ fm-seat.sh auto
[exit 0]
## same crossing under allow $25 (fresh throwaway state so the crossing wakes once)
$ fm-seat.sh extra-usage allow 25
extra-usage policy: allow up to $25, then hold
Measured against the spend the account itself reports for this seat.
[exit 0]
$ fm-seat.sh auto
[exit 0]
$ fm-seat.sh status
active seat for NEW workers: default
active profile: (ambient default login)
login state: unknown
auto-switch trigger: at or below 100% left on the active seat
destination minimum: only switch to a seat above 99% left
extra-usage policy: allow up to $25 of paid extra usage, then hold
auto-switch watch: not armed
new Claude dispatch: HELD (bin/fm-spawn.sh --ignore-seat-hold starts one task anyway)
  the active Claude seat quota could not be read, so whether this worker would run on paid extra usage is unknown, and the policy makes no guess
  this holds only work not yet started; a worker already running keeps its own seat and can still draw extra usage mid-task

live workers keep the seat they launched with:
  (no task records)

local secondmate homes declining inherited seats:
  (none - every local home takes this seat)
[exit 0]
